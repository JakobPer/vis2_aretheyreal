export class rectangle {
    constructor(x,y,w,h) {
        this.x = x
        this.y = y
        this.w = w
        this.h = h
    }

    intesects(rectangle) {
        
    }
}

export function rearrange(rectangles) {
    let x_rank = []
    let y_rank = []
    rectangles.forEach((r, i) => {
        
    });
}